# RETIRED (GATEWAY-OSS-02) — Gateway OSS publishes from betterdataco/bd-forge-main only.
# This workflow intentionally cannot publish. Mirror is docs/examples only.
# See: bd-forge-main/docs/internal/architecture/gateway-oss-01-scope-reconcile-publish-plan.md

name: Release to npm (RETIRED)

on:
  workflow_dispatch:
    inputs:
      acknowledge:
        description: 'This workflow is retired — publish from bd-forge-main oss-release.yml (GATEWAY-OSS-03)'
        required: true
        type: boolean
        default: false

jobs:
  retired:
    name: Publish path retired
    runs-on: ubuntu-latest
    steps:
      - name: Block publish
        run: |
          echo "::error::Mirror publish retired (GATEWAY-OSS-02). Canonical publish: betterdataco/bd-forge-main oss-release.yml"
          exit 1
